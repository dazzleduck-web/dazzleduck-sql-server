dazzleduck_micrometer= {
  application_id = "ap101"
  application_name = "MyApplication"
  http_endpoint = "http://localhost:8081/ingest?path=metrics"
  http.enabled = true
  grpc.enabled = false
  host = "localhost"
  port = 59307
  user = [{
    username = admin
    password = admin
  }]
}